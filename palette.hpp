#pragma once
#include <ftxui/screen/color.hpp>

namespace Pal {
    inline constexpr auto black     = ftxui::Color::RGB(0,0,0);
    inline constexpr auto green     = ftxui::Color::RGB(0,255,0);
    inline constexpr auto white     = ftxui::Color::RGB(240,240,240);
    inline constexpr auto dim_amber = ftxui::Color::RGB(180,120,0);
}